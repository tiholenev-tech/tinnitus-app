# Gradle wrapper & shell scripts must keep LF so they run on the Linux CI runner.
gradlew      text eol=lf
*.sh         text eol=lf
# Windows batch files need CRLF.
*.bat        text eol=crlf
# Binaries — never touch line endings.
*.jar        binary
*.png        binary
*.keystore   binary
