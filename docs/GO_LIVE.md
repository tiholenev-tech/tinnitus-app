# AURALIS — GO-LIVE план (какво остава до пълен старт)

Обновено: юни 2026. Маркери: ✅ готово · 🔧 мое (код) · 👤 твое · ⚖️ юрист · 🖥️ сървър.

## 🇮🇹 Италиански (статус)
- ✅ **Сайт `/it/`** — начална, 5 раздела, 9 статии (с DOI цитати), езиков превключвач BG⇄IT, hreflang.
- ✅ **Приложение на италиански — интерфейс + онбординг + квиз + FAQ** (it.json, регистриран `it`). Целият интерактивен поток е на италиански.
- 🔧 **Остава за превод (it.json):** thi (THI тест), profile_results, science (научна библиотека), cbt (14-дневна програма), categoryInfo, content, ui.diary/privacy/terms — учебно съдържание (~850 низа). Падат на BG fallback (нищо не е счупено); превеждат се на следващи вълни.
- 🔧 **Остава за сайта:** още IT статии до паритет с BG (12 от 20 BG теми).

## ✅ Готово (живо сега)
- **Публичен сайт** на `tinnitus-app.help` (auralis дизайн, меню с 5 раздела, компактен хедър).
- **20 статии** в 5 раздела — GEO стил, реални DOI/Cochrane цитати, Schema (Article + FAQ + MedicalCondition + Breadcrumb).
- **Интерактивен тест** с реален звук (чист тон + notched), безопасен.
- **SEO/GEO:** sitemap (30 URL), robots (блокира обучаващи, пуска търсещи), семантичен HTML, реален 404/500.
- **Root swap:** сайтът е на `/`, приложението на `/app.html`, инсталираното приложение е защитено (баща ти).
- **Доказателствен регистър** (`docs/research/EVIDENCE_TINNITUS.md`) за рецензента.
- **Търговски backend (написан + одитиран, дормантен):** magic-link вход, облачна синхронизация, Stripe, ePay/EasyPay, анти-фрод, dormant paywall с grandfather за заварените.

## 🖥️ За активиране на продажби (когато решиш — сървърна работа)
Следвай докъментите в `db/`: `SERVER_SETUP.md`, `STRIPE_SETUP.md`, `EPAY_SETUP.md`.
1. 🖥️ Импортирай `db/schema.sql` в MySQL на дроплета.
2. 🖥️ Копирай `api/config.sample.php` → `api/config.php` и попълни: DB данни, SMTP (за magic link), `base_url`, `magic_ttl_min`, `trial_days`, `ip_trial_limit`.
3. 🖥️ Stripe ключове + webhook (`/api/stripe_webhook.php`, secret в config). ePay ключове (min + secret).
4. 🔧 Свържи формите на сайта (Вход/Контакт) към реалните endpoint-и — сега са демо към `/blagodarya/`. (1 ред на форма, правя го щом backend-ът е жив.)
5. 🔧 Когато си готов за продажба: `PAYWALL_ENABLED = true` в `js/paywall.js` + `Paywall.guard(...)` по премиум екраните. (Заварените и баща ти остават с пълен достъп.)

## 👤 Твои задачи (нямам достъп до тях)
1. 👤 **Реален УНГ рецензент** — щом имаш име, добавям го в статиите + Schema `reviewedBy` (готов механизъм, 1 ред на статия).
2. 👤 **Google Search Console** + **Bing Webmaster Tools** → подай `sitemap.xml` (ChatGPT търси през Bing, Gemini през Google).
3. ⚖️ **Юрист** финализира Поверителност/Условия/Възстановяване — черновите са в репото (`AURALIS_TERMS_DRAFT.md`, `AURALIS_REFUND_DRAFT.md`, страниците `/poveritelnost/`, `/usloviya/`). Щом дойде текстът, го нанасям.

## 🔧 Което мога да продължа сам (кажи кое)
- Още статии (имам роудмап + може още проучвания) — напр. „видове тинитус", „хиперакузис", „тинитус при високо кръвно", „звукова травма (слушалки/концерт)".
- Италиански вариант на сайта (research-ът съдържа италиански фрази) — отделен езиков слой.
- Дребна полировка по сайта (вътрешни връзки, OG картинки, performance).
